    //TODO implement the hook with effect and state
    //TODO add index file
    
    /*useEffect( () => {
        async function getCurrentAddress () {
            const userData = await fcl.currentUser().snapshot()
            setTargetAddress(`0x${userData.addr}`)
        }
        getCurrentAddress()
    }, [NFT_ids])*/

    export default ()=> 'Dummy User'